<% @article.each do |article| %>
<div>
  <h2 class="title"><%= link_to article.title, article %></h2>
  <p class="body"><%= truncate(article.description, length:100) %></p>
  <p class="date"><%= time_ago_in_words(article.created_at) %> ago</p>
</div>
<% end %>